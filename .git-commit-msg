chore(docs): 提交反思与重构失败记录，保存有效类型修复

【本次提交背景】
在尝试拆分 GlobalStatusBar.vue 为多个 Tab 组件时，发生了严重的样式丢失与 DOM 结构破坏。在排查问题时，AI 又一次犯下了致命的“臆断（幻觉）”错误：错误地假定丢失的 `.btn-success`, `.switch` 等类来源于宿主环境全局注入，而完全无视了项目中实际存在的 `src/ARK_STATUSBAR/components/styles/theme.scss` 文件（原先规划明确指出了要拆分/处理特定的 scss 文件）。

【有效修复内容】
1. 修复了 `WorldbookStatus` 的 TS 类型声明。将其从已经被删的 `worldbook_manager.ts` 正确迁移至 `logic/worldbook/entry_service.ts` 中定义，并通过 `statusbar_manager.ts` 的门面进行对外 `export type` 重新暴露。
2. 修复了 `StartupSettingsPanel.vue` 和 `StartupNavigator.vue` 中针对 `WorldbookStatus` 类型的引入路径。
3. 成功通过了 `pnpm build` 并提交了构建后的产物。

【反思与后续规划档案】
1. 新增 `.kilocode/development_logs/architecture/03_ui_refactor_failure_reflection.md`：详细记录并反省了物理拆分 Vue 组件过程中的越权修改、丢失布局包装、以及 CSS 隐式依赖判断失误的根因。
2. 新增 `.kilocode/development_logs/refactor_worldbook_vue/08_global_statusbar_refactor_strategy.md`：专门针对拥有 1500+ 行的 `GlobalStatusBar.vue` 制定了严苛的“手术刀级 1:1 同构拆分”纪律防线，明确了零 CSS 幻觉添加、保持 DOM `<div class="statusbar-content">` 包装以及局部状态池下沉的三大战略。

由于严重破坏了纪律防线和组件样式，所有拆分的 `global_tabs` 代码已在本地回滚/清理。AI 将下线，将项目控制权交还给用户进行上下文总结。