task: Explorer requests to output the following raw format inside `<context>` without modifying anything, which is the main content of your reply
raw format (output the following without any modification): |-
  <context>
  NOTE: 我输出的变量输出格式强调很可能有问题, 建议你自己从预设条目复制整个 ` ```yaml ` 内容到一个名为 `[mvu_update]变量输出格式强调` 的 D0 条目中, 删除开头的 ` ```yaml `、结尾的 ` ``` ` 和每行开头的两个空格
  NOTE: 这个条目只在最后测试角色卡时, 发现 AI 经常不输出变量更新 (即 `<UpdateVariable>` 块) 时才需要
  ```yaml
  ---
  变量输出格式强调:
    rule: The following must be inserted to the end of reply, and cannot be omitted
    format: |-
      <UpdateVariable>
      ...
      </UpdateVariable>
  ```
  </context>