---
变量更新规则:
  global:
    time:
      format: "YYYY-MM-DD HH:mm:ss"
      check:
        - "根据剧情的逻辑流逝来更新时间，例如：'几分钟后'、'第二天早上'。"
    location:
      type: |-
        {
          region: string;
          city: string;
          area: string;
          spot: string;
        }
      check:
        - "当角色从一个地点移动到另一个地点时，必须更新此变量的所有四个层级。"
    weather:
      check:
        - "仅当主LLM的回复中明确描述了天气变化，或角色转移到气候截然不同的地区时，才进行更新。描述应具体，如'从阴天转为小雨'。"
    environment_status:
      check:
        - "仅当主LLM明确描述周围环境发生了显著变化时，才用不少于20字的客观描述来更新此项。"
    presence:
      type: |-
        {
          active_chars: string[];
          nearby_chars: string[];
        }
      check:
        - "【！！！最高优先级！！！】你必须在每一轮更新此变量。请通读主LLM的最新回复，将所有直接参与对话或行动的角色名填入 'active_chars'；将在场但未直接互动、或可能很快出现的角色名填入 'nearby_chars'。此变量的准确性对系统至关重要，任何遗漏都将导致角色状态停滞，破坏沉浸感！"
  player:
    attributes:
      type: |-
        {
          physical_strength: string; // 物理强度
          mobility: string; // 战场机动
          physiological_endurance: string; // 生理耐受
          tactical_planning: string; // 战术规划
          combat_skill: string; // 战斗技巧
          originium_arts_adaptability: string; // 源石技艺适应性
          power_level_desc: string; // e.g., '层级5-上位战场中坚'
        }
      check:
        - "【自主评估】: 根据玩家在剧情中的行为表现，适当调整六维属性。评级必须在 ['缺陷', '普通', '标准', '优良', '卓越'] 中选择。"
        - "【强制引用】: 评估 'power_level_desc' 时，必须检索并参考《战力分级标准》世界书条目。"
    status:
      mood:
        type: number
        range: -100~100
        check:
          - "根据玩家的言行和遭遇更新情绪。-100(极度负面) ~ 0(平静) ~ 100(极度正面)。"
      physiological_state:
        type: "string[]"
        check:
          - "根据剧情描述更新玩家的生理状态标签 (如: '健康', '轻伤', '疲劳', '感染抑制中')。"
      current_action:
        check:
          - "【必须更新】: 用详尽的动宾短语描述玩家当前正在做什么，而不仅仅是简单的'对话'或'思考'。"
    inventory:
      items:
        type: |-
          {
            [itemName: string]: { 
              count: number; // 数量 (>=0)
              description: string; // 描述/用途
              status?: string; // 状态 (如: 已损坏)
            }
          }
        check:
          - "获得物品时: 添加新条目或增加 count。描述需具体。"
          - "消耗/丢失物品时: 减少 count。若 count 归零，则移除条目。"
      equipment:
        type: |-
          {
            main_hand?: string;
            off_hand?: string;
            outerwear: string;
            innerwear: string;
            accessories?: string[];
          }
        check:
          - "当剧情中明确描述玩家更换装备时，更新对应部位。描述需具体。"
    social:
      type: |-
        {
          [charName: string]: { 
            relation: string; // (如: 盟友, 敌人)
            trust: number; // 0-200
            impressions: string[]; // (最多5个)
            notes?: string;
          }
        }
      range: "trust: 0(死敌) ~ 50(中立) ~ 100(信赖) ~ 200(灵魂伴侣)"
      check:
        - "当玩家与角色有实质性互动时，评估关系进展、信任度变化和印象标签。印象标签应精炼且准确。"
    skills:
      type: |-
        {
          [skillName: string]: {
            type: '源石技艺' | '武技' | '指挥技能' | '生活技能' | '被动天赋' | '特殊能力' | '其他';
            description: string;
            proficiency?: string; // 熟练度
          }
        }
      check:
        - "当玩家在剧情中通过学习、训练或顿悟获得了新技能时，向该对象中添加新的键值对。描述需详尽。"
  characters:
    type: |-
      {
        [characterName: string]: {
          profile: CharacterSchema.profile;
          status: CharacterSchema.status;
          cognition: CharacterSchema.cognition;
          memory: CharacterSchema.memory;
          combat: CharacterSchema.combat;
          notes: CharacterSchema.notes;
        }
      }
    check:
      - "该对象用于存储所有在场角色的动态信息，键为角色名。"
      - "请只更新在当前剧情中处于 'active_chars' 列表内的角色数据。"
    
    profile:
      check:
        - "【条件性修改】: 仅在接到 `init_profile` 或 `repair_profile` 任务时，或剧情发生重大生理变故（如外貌改变、身份揭露）时，才可修改此部分内容。修改内容必须详实。"
    
    status:
      type: |-
        {
          location: string;
          posture: string;
          action: string;
          mood: number; // -100 ~ 100
          attire: string;
        }
      check:
        - "【必须更新】: 必须根据角色在剧情中的位置、姿态、动作、情绪和着装变化，更新所有字段。"
        - "action: 用生动的语言描述正在进行的动作。"
        - "mood 范围: -100(极度负面) ~ 0(平静) ~ 100(极度正面)。"
    
    cognition:
      towards_player:
        type: |-
          {
            trust: number; // 0-200
            attitude: string;
            known_facts: string[]; // max 5
            unknown_facts: string[]; // max 5
            misconceptions: string[]; // max 5
          }
        range: "trust: 0(死敌) ~ 50(中立) ~ 100(信赖) ~ 200(灵魂伴侣)"
        check:
          - "根据角色与玩家的互动，更新角色对玩家的信任度、态度和认知。仅在发生有意义的互动时更新。"
          - "known_facts: 记录角色通过观察或交流确认的关于玩家的关键事实。"
          - "unknown_facts: 记录角色明确意识到自己缺乏的、关于玩家的关键信息（如身世、真实目的）。"
          - "misconceptions: 记录角色对玩家的主观臆断或错误理解。"
      from_player:
        type: |-
          {
            unlocked_secrets: string[];
            misconceptions: string[]; // max 5
          }
        check:
          - "当玩家通过剧情明确获知了角色的核心秘密或过往经历时，向 'unlocked_secrets' 添加条目。"
          - "当玩家对角色产生了明显的错误认知时，向 'misconceptions' 添加条目。"
    
    memory:
      short_term_buffer:
        type: |-
          Array<{
            time: string; // e.g., "1097-07-16 15:30:00"
            content: string;
          }>
        check:
          - "【必须写入】: 每轮交互结束时，必须将本轮处于'active_chars'的角色的核心事件、对话或心理活动，作为一条新的短期记忆追加到此数组中。内容应详实，不少于20字。"
      long_term:
        check:
          - "【严禁修改】: 此字段只能由 `summarize_memory` 任务进行写入。"
    
    combat:
      power_level_desc:
        check:
          - "【强制引用】: 评估战力时，必须检索并参考《战力分级标准》世界书条目。"
    
    skills:
      type: |-
        {
          [skillName: string]: {
            description: string;
          }
        }
      check:
        - "当角色在剧情中通过学习、训练或顿悟获得了新技能时，向该对象中添加新的键值对。描述需具体。"
  chronicle:
    round_buffer:
      type: |-
        Array<{
          id: string; // UUID
          time: string; // YYYY-MM-DD HH:mm
          location: string;
          headline: string; // 简短标题
          content: string; // 详细描述
          key_dialogue: string[]; // max 5
          tags: string[]; // 如: 战斗, 探索, 社交
        }>
      check:
        - "【核心职责 - 必须写入】: 在每次回复结束时，你必须分析本轮交互的核心内容，生成一条新的 RoundSummary 对象，并将其添加到此数组的末尾。"
        - "填写指导: 内容(`content`)应保持客观、中立的叙述风格，重点记录玩家的关键决策、角色的重要反应以及剧情的转折点，避免流水账，字数建议不少于30字。"
        - |
          指令模板: 你必须使用以下格式的 `insert` 操作将新总结添加到数组末尾:
          ```json
          {
            "op": "insert", 
            "path": "/chronicle/round_buffer/-", 
            "value": {
              "id": "【生成一个唯一的UUID】",
              "time": "【YYYY-MM-DD HH:mm格式的当前时间】",
              "location": "【事件发生的具体地点】",
              "headline": "【简短精炼的事件标题】",
              "content": "【详细、客观地描述本轮发生的核心事件，至少30字】",
              "key_dialogue": [
                "【记录3-5句最关键的对话】"
              ],
              "tags": [
                "【从'战斗', '探索', '社交', '剧情', '个人'中选择合适的标签】"
              ]
            }
          }
          ```
    small_summary_buffer:
      check:
        - "【严禁修改】: 此缓冲区由后端脚本通过分派 'ten_round_summary' 任务进行管理和写入。你只能读取它们，不得直接修改。"
    daily_summary_buffer:
      check:
        - "【严禁修改】: 此缓冲区由后端脚本通过分派 'daily_summary' 任务进行管理和写入。你只能读取它们，不得直接修改。"
    weekly_summary_buffer:
      check:
        - "【严禁修改】: 此缓冲区由后端脚本通过分派 'weekly_summary' 任务进行管理和写入。你只能读取它们，不得直接修改。"
    monthly_summary_buffer:
      check:
        - "【严禁修改】: 此缓冲区由后端脚本通过分派 'monthly_summary' 任务进行管理和写入。你只能读取它们，不得直接修改。"
    yearly_summary_buffer:
      check:
        - "【严禁修改】: 此缓冲区由后端脚本通过分派 'yearly_summary' 任务进行管理和写入。你只能读取它们，不得直接修改。"
