# PoC (Proof of Concept) 测试目录

本目录专用于存放各种前置功能的概念验证脚本。

## 测试规范
1. **统一使用 `.js` 后缀**：方便直接全选复制，粘贴到酒馆系统的开发者控制台 (F12) 或以独立脚本形式挂载测试。
2. **状态记录**：每次测试结束后，必须在对应的 `.js` 文件头部注释或 `.md` 规划文件中同步测试结果。
3. **隔离性**：PoC 代码仅用于验证逻辑可行性，验证通过后，将重构为 Vue 组件或模块化 TS 代码并入正式项目。